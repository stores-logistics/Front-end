version: '2.1'

services:
  docker-app:
    container_name: SL-FrontEnd
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - '.:/usr/src/app'
      - '/usr/src/app/node_modules'
    ports:
      - '5001:5001'
    environment:
      - NODE_ENV=development